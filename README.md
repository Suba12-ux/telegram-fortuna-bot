# 🔮 Telegram Fortune Bot — Бот-предсказатель

Telegram-бот, который генерирует случайные предсказания и хранит историю для каждого пользователя. Написан на Python с использованием асинхронной библиотеки python-telegram-bot, база данных — SQLite.

## 🛠 Стек технологий

### Backend
* **Python 3.11+** — основной язык разработки
* **python-telegram-bot v20.x** — современная асинхронная библиотека для Telegram Bot API

### База данных
* **SQLite** — лёгкая реляционная БД, идеально для стартапов
* **sqlite3** — стандартная библиотека Python для работы с SQLite

### Контейнеризация
* **Docker** — для упаковки приложения в контейнер
* **Docker Compose** — для управления контейнерами
* **Dockerfile** — инструкция по сборке образа

### Вспомогательные библиотеки
* **python-dotenv** — загрузка переменных окружения из .env
* **random** — для генерации случайных предсказаний
* **os и datetime** — для работы с файлами и временем

### Инфраструктура
* **Ubuntu Server 22.04 LTS** — ОС на сервере
* **Docker Engine** — для запуска контейнеров
* **Git + GitHub** — контроль версий и хостинг кода

# Безопасность
Переменные окружения в .env (токен бота)

.gitignore — чтобы секретные файлы не попали в репозиторий

Non-root пользователь в Docker — для минимальных привилегий

# Структура проекта

```bash
telegram-fortune-bot/
├── app/                    # Основная логика приложения
│   ├── __init__.py
│   ├── main.py            # Точка входа, инициализация бота
│   ├── handlers.py        # Обработчики команд Telegram
│   ├── database.py        # Работа с SQLite
│   └── data.py            # Список предсказаний
├── data/                  # Директория для БД (volume)
│   └── fortunes.db        # Файл базы данных
├── .env                   # Переменные окружения (не в репозитории!)
├── .env.example           # Пример .env файла
├── .gitignore
├── requirements.txt       # Зависимости Python
├── Dockerfile            # Сборка Docker-образа
└── docker-compose.yml    # Конфигурация для Compose
```

## 🚀 Установка и запуск

### 1. Получение токена бота
Создайте бота через [@BotFather](https://t.me/BotFather) и сохраните полученный токен.

### 2. Клонирование репозитория
```bash
git clone https://github.com/ваш-username/telegram-fortune-bot.git
cd telegram-fortune-bot